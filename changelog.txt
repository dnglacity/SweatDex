[v1.23] 03.08.2026 Core Match Format Template Library

- Added: Global `core_match_format_templates` Supabase table with RLS (read-only for authenticated users)
- Added: `getCoreMatchFormatTemplates()` in PlayerService fetching global templates ordered by sport and name
- Added: "★ Core Library" section in the Match Format picker sheet, shown above team formats
- Added: `sport` field to `MatchFormatTemplate` model for display in the picker
- Changed: When a core template is selected, it is auto-copied into the team's own templates (or reused by name if a copy already exists)
- Added: `_SectionHeader` widget used in the format picker to label Core Library and My Formats sections

[v1.23] 03.08.2026 MatchView: Start Match side-by-side roster view

- Added: "Start Match" floating button stacked above "Staged" FAB, visible to the host when both teams are staged
- Added: Confirmation dialog before starting the match
- Added: MatchLiveScreen showing both teams' game rosters side by side with match format sections, position slots, starters, and substitutes
- Added: get_linked_match_roster SECURITY DEFINER RPC that fetches the opponent's selected roster with enriched player names, format template sections, and enriched format slot assignments
- Added: getLinkedMatchRoster() method in PlayerService calling the new RPC

[v1.23] 03.08.2026 MatchView: sync host settings to guest in real time

- Fixed: Match settings edits (date, notes, home/away, team name) by the host were not propagated to the guest team's mirror row — guest saw stale data
- Added: update_match_settings SECURITY DEFINER RPC that updates both the host's and guest's match rows atomically (host my_team_name → guest opponent_name, is_home inverted, date/notes mirrored)
- Added: updateMatchSettings() method in PlayerService calling the new RPC
- Changed: _openEditSheet() now calls updateMatchSettings() instead of updateMatch() and sends a match_settings_updated broadcast event
- Changed: postgres-changes callback now handles field updates for guests (my_team_name, opponent_name, match_date, is_home, notes, is_staged)
- Added: match_settings_updated broadcast listener as a fallback for RLS-blocked postgres-changes
- Changed: _refreshMatchRow() now fetches full settings columns (not just is_staged/linked_match_id) so guest refresh picks up all changes

[v1.22] 03.05.2026 Game roster: return-to-available drop zone

- Added: "Drop here to return to Available" DragTarget strip below the Substitutes Bench in the Roster tab of GameRosterScreen
- Changed: Dragging any starter or substitute onto the new drop zone removes them from the roster and returns them to the Available tab
- Changed: Drop zone highlights orange with an undo icon on hover; only accepts players currently assigned to starters or substitutes

[v1.22] 03.03.2026 2026 CSV roster import

- Added: CsvImportScreen (lib/screens/csv_import_screen.dart) — coaches can upload a .csv file to bulk-populate their team roster
- Added: Template CSV download (copies template to clipboard on mobile/web; includes all supported columns with example rows)
- Added: CSV preview table with per-row validation — invalid rows (missing first_name or last_name) are highlighted and skipped on import
- Added: Summary banner showing how many rows are ready vs have errors before importing
- Added: bulkAddPlayers(List<Player>) method in PlayerService — inserts rows one at a time and aggregates errors without aborting the entire batch
- Added: file_picker ^8.0.0+1 and csv ^6.0.0 packages to pubspec.yaml
- Changed: RosterScreen overflow menu — new "Import Roster from CSV" item (coach/owner only) navigates to CsvImportScreen and refreshes the roster on success

v1.22 03.03.2026 MatchView: live checkmark updates via broadcast channel

- Added: Supabase Realtime broadcast channel (match-events-{TeamA_match_id}) for cross-team real-time signaling, bypassing RLS restrictions on postgres-changes
- Added: _broadcastChannel field; subscribed in _subscribeToMatchStaged alongside existing postgres-changes subscription
- Changed: Stage and Unstage actions now send staged_update broadcast so Team B sees Team A's checkmark update instantly without refresh
- Changed: Submit Roster action now sends guest_submitted broadcast so Team A sees Team B's checkmark update instantly without refresh
- Fixed: Team B was not receiving live is_staged updates from Team A because RLS blocked postgres-changes on Team A's row; broadcast channel resolves this
- Changed: dispose() now unsubscribes both _matchChannel and _broadcastChannel

[v1.22] 03.02.2026 1907 MatchView: green checkmark on guest team name after roster submission

- Added: guest_roster_submitted boolean column to matches table (stored on Team A's row)
- Added: submit_guest_roster SECURITY DEFINER RPC — validates caller is a guest coach/owner, sets guest_roster_submitted = true on the linked host row
- Added: PlayerService.submitGuestRoster(matchId) calls the new RPC
- Added: Match model guestRosterSubmitted field; included in fromMap/copyWith
- Changed: "My Team" block checkmark logic — match owner shows when staged; guest shows when they submitted roster
- Changed: "Opponent" block checkmark logic — match owner sees checkmark when guest submitted; guest sees checkmark when Team A staged
- Changed: Submit Roster button now calls submitGuestRoster and updates local _guestRosterSubmitted state
- Changed: _refreshMatchRow fetches guest_roster_submitted for Team A's initial load
- Changed: Realtime callback updates _guestRosterSubmitted when Team A's row changes

v1.22 03.03.2026 MatchView: guest team can leave a match

- Added: "Leave Match" option in the overflow menu for guest (Team B) match views; styled red with exit_to_app icon
- Added: Confirmation dialog warns that the match will be removed and a new invite is needed to rejoin
- Added: PlayerService.leaveMatch(matchId) calls new leave_match SECURITY DEFINER RPC
- Added: leave_match RPC — validates caller is a coach/owner of the guest team, clears linked_match_id and is_staged on the host row, deletes the guest match row
- Changed: Leaving the match pops with null so the matches list removes it immediately

v1.22 03.02.2026 MatchView: fix staged checkmark appearing on wrong team for Team B

- Fixed: Team B's match view incorrectly showed the green checkmark on "My Team" (Team B) instead of "Opponent" (Team A) when only Team A had staged
- Fixed: _isMatchOwner getter was using currentTeamId == _match.teamId, which was always true for Team B since each team's match row uses their own team ID; replaced with !_match.isGuestMatch which correctly identifies non-owners

v1.22 03.02.2026 Fix redeem_match_invite using wrong column name

- Fixed: redeem_match_invite RPC referenced display_name instead of team_name on teams table, causing "column does not exist" error when joining a match by code

v1.22 03.02.2026 MatchView: hide match settings and invite options from guest team

- Added: is_guest_match BOOLEAN column to matches table (default false)
- Changed: redeem_match_invite RPC sets is_guest_match = true on the match row created for the accepting team (Team B)
- Changed: Match model includes isGuestMatch field, populated from DB column
- Fixed: Guest team (Team B) no longer sees the overflow menu (Match Settings, Create Invite, Remove Opponent) in MatchViewScreen
- Fixed: Guest team (Team B) no longer sees the Select Roster icon button in the AppBar
- Changed: Visibility of AppBar actions now gates on !match.isGuestMatch instead of _isMatchOwner (which was always true for each team's own row)

v1.22 03.02.2026 MatchView: populate opponent name from Team B's actual team name

- Fixed: Opponent block in MatchViewScreen showed the placeholder name Team A typed at match creation instead of Team B's real team name
- Fixed: redeem_match_invite RPC now sets Team B's my_team_name from teams.team_name (actual DB team name) instead of copying opponent_name from Team A's row
- Fixed: Team A could not read Team B's match row due to matches RLS policy (team_members check) — added get_linked_match_team_name SECURITY DEFINER RPC that resolves the linked match's my_team_name and returns it to Team A safely
- Added: _linkedTeamName state in MatchViewScreen, populated on load and via Realtime when linked_match_id first appears
- Changed: Opponent _TeamBlock shows "Awaiting opponent..." (italic, muted) when no opponent has joined; shows linked team name once invite is accepted
- Changed: AppBar title updates to reflect the real opponent name after invite acceptance

v1.22 03.02.2026 MatchView: remove opposing team + fix Team B staged checkmark

- Added: "Remove Opposing Team" option in match owner's overflow menu (visible only when an opponent has joined via invite); styled red with link_off icon
- Added: Confirmation dialog before removing — warns that opponent's match record will be deleted and a new invite will be needed to rejoin
- Added: remove_opposing_team SECURITY DEFINER RPC — validates caller is coach/owner, deletes the opposing team's mirror match row, clears linked_match_id and resets is_staged on the owner's row
- Added: PlayerService.removeOpposingTeam(matchId) calls the new RPC
- Fixed: After removal, local state clears linkedMatchId and _isStaged, and Realtime re-subscribes to the owner's own row
- Fixed: Team B was not seeing Team A's green checkmark — root cause was that each team has its own mirrored match row; Team B's row is_staged was never updated when Team A staged
- Fixed: Match model now includes linkedMatchId field (parsed from linked_match_id column)
- Changed: getMatches query now selects linked_match_id
- Changed: When Team B opens a match (!_isMatchOwner && linkedMatchId != null), MatchViewScreen fetches initial is_staged from Team A's linked row and subscribes Realtime to that row instead of its own mirror
- Changed: "My Team" checkmark now only shows for the match owner (was incorrectly showing for Team B when Team A had staged)

v1.22 03.02.2026 MatchView: fix staging checkmark visibility and add undo staging
- Fixed: Team B incorrectly saw checkmark next to their own name when Team A staged — checkmark now always renders on the match owner's team block (myTeamName) regardless of viewer
- Fixed: Team B's realtime subscription never fired when Team A staged — matches are mirrored rows with no link; added linked_match_id column and SECURITY DEFINER RPCs stage_match / unstage_match that update both rows atomically
- Changed: redeem_match_invite RPC updated to populate linked_match_id bidirectionally on both match rows when an invite is redeemed
- Added: unstage_match RPC (SECURITY DEFINER) sets is_staged = false on both linked rows
- Changed: PlayerService.stageMatch and unstageMatch now call stage_match / unstage_match RPCs instead of direct table updates
- Added: Stage Match FAB is now re-pressable when already staged; pressing it shows "Undo Staging" confirmation dialog and calls unstageMatch()
- Changed: Realtime callback now reflects both true and false values for is_staged (previously only handled true)
- Changed: Supabase_script.md updated with full migration (ALTER TABLE, updated redeem_match_invite, new stage_match / unstage_match RPCs, GRANT statements)

v1.22 03.02.2026 MatchView: stage match sets checkmark on both teams' screens
- Added: is_staged boolean column to matches table (see Supabase_script.md for migration)
- Changed: Match model gains isStaged field (fromMap reads is_staged, copyWith supports it)
- Added: PlayerService.stageMatch(matchId) sets is_staged = true on the DB row
- Changed: getMatches select now includes is_staged column
- Changed: Stage Match FAB no longer navigates to MatchPlayScreen; instead calls stageMatch() and sets local _isStaged = true; button becomes disabled and shows "Staged" label after staging
- Added: Supabase Realtime subscription in MatchViewScreen watches for is_staged updates on the current match row — opposing team's screen reflects the checkmark in real time
- Added: green checkmark icon to the left of the team name in _TeamBlock when showCheckmark is true; match owner sees it next to "My Team", opposing team sees it next to "Opponent"
- Changed: match_view_screen.dart imports supabase_flutter for RealtimeChannel; disposes channel in dispose()

v1.22 03.02.2026 MatchView: show format sections, position assignments, and player names
- Added: _rosterFormat and _formatSlots state vars to _MatchViewScreenState
- Changed: _loadRosterPreview now fetches match_format_template_id and format_slots from the roster row, loads the MatchFormatTemplate via getMatchFormatTemplateById, and enriches starters/subs slots with player name and position by joining against getPlayers
- Fixed: starters and subs preview was showing blank names — the DB stores slot objects {player_id, slot_number, position_override}, not full player records; enrichment now resolves names correctly
- Changed: _RosterPreviewPanel accepts format (MatchFormatTemplate?) and formatSlots (Map<String, String>) parameters
- Added: format sections block in _RosterPreviewPanel — renders format name label, then each section as a card with colored header and numbered position rows showing assigned player name + position (or — if unassigned); displayed above the starters and subs lists

v1.21 03.02.2026 MatchView: inline roster preview + persist selected roster to DB
- Added: selected_roster_id column to matches table (see Supabase_script.md for migration)
- Changed: Match.fromMap() now reads selected_roster_id from DB; comment updated to reflect field is persisted
- Changed: PlayerService.updateMatch() accepts optional selectedRosterId parameter (uses sentinel so null can be passed to clear)
- Changed: getMatches() select query includes selected_roster_id column
- Added: _RosterPreviewPanel widget in match_view_screen.dart — shows starters and subs list inline below the My Team vs Opponent section when a roster is selected
- Added: _PlayerRow helper widget for player name + position display in the preview panel
- Added: _loadRosterPreview() in _MatchViewScreenState — fetches roster data via getGameRosterById on init and after selection
- Added: _openRosterInEditor() in _MatchViewScreenState — opens selected roster in GameRosterScreen and reloads preview on return
- Changed: _showSelectRosterSheet now persists selection to DB via updateMatch and loads the roster preview after confirming
- Changed: initState loads roster preview if match already has a selected_roster_id

v1.20 03.02.2026 Fix: format slot assignments now persist across roster sessions
- Fixed: format-template position assignments (player-to-slot) were not saved when hitting Save on the Roster tab; they now persist and restore correctly when reopening a saved roster
- Added: format_slots jsonb column to game_rosters table (see Supabase_script.md for migration)
- Changed: updateGameRosterLineup in player_service.dart accepts new optional formatSlots parameter (Map<String, String>: slot key → player UUID)
- Changed: _saveRoster in game_roster_screen.dart passes _formatSlots serialized as player IDs
- Changed: _loadPlayers in game_roster_screen.dart restores _formatSlots from rosterData['format_slots'] after loading player map

v1.19 03.02.2026 Add Claude Code subagents for Flutter, debugging, code review, PostgreSQL, and security
- Added: .claude/agents/flutter-expert.md — Flutter 3+ specialist subagent for cross-platform UI, state management, native integrations, and performance optimization
- Added: .claude/agents/debugger.md — systematic debugging subagent for root cause analysis, stack trace investigation, and issue resolution
- Added: .claude/agents/code-reviewer.md — comprehensive code review subagent covering quality, security vulnerabilities, and best practices
- Added: .claude/agents/postgres-pro.md — PostgreSQL/Supabase expert subagent for query optimization, schema design, and RLS
- Added: .claude/agents/security-auditor.md — security audit subagent for vulnerability assessment and auth/RLS review

v1.18 03.02.2026 Match Format in Match View, cross-zone guard, DRY snackbar/controller refactor
- Added: Match Format option in _MatchSettingsSheet (match_view_screen.dart) — opens _MatchFormatPickerSheet listing team templates with edit/select/create actions; tapping a template applies it to the match context
- Added: _MatchFormatPickerSheet in match_view_screen.dart — DraggableScrollableSheet that loads format templates, supports edit via EditFormatSheet, and navigates to MatchFormatScreen for new formats
- Added: onMatchFormat callback to _MatchSettingsSheet and _openMatchFormat() handler in _MatchViewScreenState
- Fixed: cross-zone drag guard in GameRosterScreen — _addToStarters now blocks if the player is already on the bench (and vice versa), preventing a player from appearing on both sides simultaneously; shows an info snackbar on violation
- Changed: Match model — added selectedRosterId and selectedRosterName as in-memory-only fields (not persisted to DB) with sentinel-pattern copyWith support
- Changed: ~50 inline ScaffoldMessenger.showSnackBar constructions across login_screen, reset_password_screen, account_settings_screen, add_player_screen, manage_members_screen, game_roster_screen, saved_roster_screen, team_selection_screen, roster_screen, match_view_screen replaced with showSuccessSnackBar / showInfoSnackBar / showErrorSnackBar helpers from utils/ui_helpers.dart
- Changed: dispose() overrides in login_screen and reset_password_screen collapsed to .disposeAll() extension
- Changed: calendar_screen and roster_screen now import kMonthNames/kShortMonthNames/kWeekdayLabels from constants/date_constants.dart; local duplicate const arrays removed

v1.17 03.02.2026 Fix cross-zone drag and blue hover glow on Roster tab
- Fixed: starters could not be dragged to the Substitutes zone — root cause was SingleChildScrollView scrolling the subs zone out of viewport during drag, making it unreachable
- Changed: _RosterTabView layout replaced with a Column of two Expanded halves (starters top, subs bottom) so both zones are always on-screen simultaneously
- Changed: ReorderableListView inside each zone no longer uses shrinkWrap/NeverScrollableScrollPhysics; scrolls within its own Expanded parent
- Added: SizedBox.expand wraps each zone's content so the full area registers as a drop target
- Added: blue glow box shadow + blue border on both drop zones when a dragged player hovers over them
- Fixed: bracket error in _SubsDropZone.build from incomplete prior edit

v1.16 03.01.2026 Duplicate player positions in Roster tab
- Added: players in the Roster tab can now be dragged onto multiple positions within the same side (starters or substitutes)
- Added: yellow warning badge with occurrence count shown next to player name when assigned to more than one position
- Changed: _StarterDropZone and _SubsDropZone now accept drops of already-assigned players (creates duplicate), but block cross-side drops (player can't be on both starters and subs simultaneously)
- Changed: _addToStarters and _addToSubs validate cross-side constraints before adding
- Changed: promote/demote actions (_promoteSubToStarter, _demoteStarterToSub) now remove ALL occurrences of a player from the source side before moving
- Changed: ReorderableListView item keys changed from player-id-based to index-based to support duplicate player entries without key collisions
- Added: appearanceCount parameter to _RosterRowTile for warning badge rendering

v1.14 03.01.2026 Fix match format not persisting across game roster sessions
- Fixed: match format selection was lost when saving and reopening a game roster
- Added: match_format_template_id column to game_rosters table (nullable FK to match_format_templates)
- Changed: updateGameRosterLineup now accepts and saves matchFormatTemplateId
- Added: getMatchFormatTemplateById in PlayerService to fetch a single template by ID
- Changed: _loadPlayers in GameRosterScreen restores _activeFormat from saved match_format_template_id on load

v1.15 03.01.2026 Match Format integration in Game Roster
- Added: "Match Format" button in the bottom-left of GameRosterScreen — always visible
- Added: _MatchFormatPickerSheet — bottom sheet that loads and lists saved match format templates; tapping one applies it to the Roster tab; "New" button navigates to MatchFormatScreen to create templates
- Added: Format-based split layout in _RosterTabView — when a format is active, Roster tab shows sections as headers with numbered position slots on the left and starters/substitutes list on the right
- Added: _FormatPositionSlot widget — tappable slot that opens a player picker (from unassigned starters/subs) when empty, or clears the assignment when tapped with a player assigned
- Added: _FormatPlayerTile widget — shows a player in the right-side list, dimmed with a check when already assigned to a slot
- Added: Clear/dismiss button next to the active format name in the bottom bar to remove the format and return to standard layout
- Changed: bottomNavigationBar now always renders (previously only shown when onCancel != null) to host the Match Format button

v1.13 03.01.2026 Match Format Templates
- Added: screens/match_format_screen.dart — full screen listing team Match Format templates with a "+" AppBar button to create new ones; each template has a name and flexible sections (title + position count)
- Added: MatchFormatTemplate and MatchFormatSection models in match_format_screen.dart
- Added: PlayerService.getMatchFormatTemplates(), createMatchFormatTemplate(), deleteMatchFormatTemplate() service methods
- Added: "Match Format" ListTile in _MatchSettingsSheet (match_view_screen.dart) that navigates to MatchFormatScreen
- Added: Supabase_script.md — SQL for match_format_templates table with RLS policy (coaches/owners/managers only)

v1.14 03.01.2026 DRY refactor — shared utilities and constants
- Added: constants/date_constants.dart — shared kMonthNames, kShortMonthNames, kDayNames, kWeekdayLabels constants
- Added: utils/ui_helpers.dart — showSuccessSnackBar, showErrorSnackBar, showInfoSnackBar helper functions
- Added: utils/validation_utils.dart — shared isPasswordStrong() function
- Added: utils/controller_utils.dart — List<TextEditingController>.disposeAll() extension
- Removed: duplicate _monthNames/_shortMonthNames/_dayNames/_weekdayLabels static const arrays from calendar_screen.dart, matches_screen.dart, roster_screen.dart, match_view_screen.dart
- Removed: duplicate _isPasswordStrong() methods from login_screen.dart and reset_password_screen.dart
- Changed: ~40 inline ScaffoldMessenger.showSnackBar constructions replaced with helpers across login_screen, reset_password_screen, account_settings_screen, add_player_screen, manage_members_screen, game_roster_screen, saved_roster_screen, team_selection_screen, roster_screen, match_view_screen
- Changed: dispose() overrides in login_screen, reset_password_screen, account_settings_screen, add_player_screen collapsed to .disposeAll()

v1.14 03.01.2026 Persist selected game roster in match view
- Fixed: screens/match_view_screen.dart — reopening the Select Roster sheet now pre-selects the previously confirmed roster; added _selectedRosterId field to _MatchViewScreenState and initialSelectedId parameter to _SelectRosterSheet; Confirm now pops a ({String id, String name}) record so both id and name are stored; parent state only updates on explicit confirmation

v1.14 03.01.2026 Add stage match confirmation and match play screen
- Added: screens/match_play_screen.dart — new screen opened when a match is staged; displays match title and location label as a live-match placeholder scaffold
- Changed: screens/match_view_screen.dart — Stage Match FAB now shows a confirmation dialog ("The match will be staged and rosters will be exchanged.") with Cancel and Stage buttons; pressing Stage navigates to MatchPlayScreen

v1.14 03.01.2026 Add Select Roster picker to match view
- Added: screens/match_view_screen.dart — clipboard icon button (coaches only) in AppBar left of menu opens a Select Roster bottom sheet; sheet streams the team's game rosters in real time, allows single selection with a green checkmark, includes a + button to create a new roster inline (opens GameRosterScreen immediately), and returns the selected roster title on Confirm
- Added: screens/match_view_screen.dart — _RosterEntry local model and _SelectRosterSheet StatefulWidget to support the picker
- Changed: screens/match_view_screen.dart — _TeamBlock now accepts optional rosterLabel; confirmed roster name displays under My Team name in the versus area with a small clipboard icon; Opponent side reserved for future roster display

v1.14 03.01.2026 Rename Matches tab to Events and expand Add Event FAB
- Changed: screens/roster_screen.dart — bottom nav label "Matches" renamed to "Events"
- Changed: screens/roster_screen.dart — replaced single "Create Event" FAB with an expandable "Add Event" FAB; tapping reveals two sub-buttons: "Use Code" (opens join-by-code dialog) and "New Event" (opens create event sheet); FAB collapses on selection or tab switch

v1.14 03.01.2026 Fix ambiguous code column in get_or_create_match_invite RPC
- Fixed: Supabase_script.md — get_or_create_match_invite now uses table alias (mi.code) in the uniqueness loop to resolve PostgreSQL error 42702 ("column reference code is ambiguous") caused by the RETURNS TABLE output column also named "code"

v1.13 03.01.2026 Add match invite code system
- Added: Supabase_script.md — SQL for public.match_invites table with RLS and three SECURITY DEFINER RPCs: get_or_create_match_invite, revoke_match_invite, redeem_match_invite; codes are single-use and deactivated immediately upon redemption
- Added: services/player_service.dart — getOrCreateMatchInvite, revokeMatchInvite, redeemMatchInvite service methods
- Added: screens/match_view_screen.dart — "Create Invite" popup menu item (coaches/owners only); _showMatchInviteDialog displays a 6-char code with Copy and End Invite actions; added isCoach parameter
- Changed: screens/matches_screen.dart — added isCoach and onMatchAdded parameters; passes isCoach to MatchViewScreen
- Changed: screens/roster_screen.dart — passes isCoach and onMatchAdded to MatchesScreen; added "Join Match by Code" overflow menu item (coaches/owners only) and _showJoinMatchDialog for redeeming a match invite code

v1.12 03.01.2026 Fix matches not persisting across sessions
- Fixed: screens/roster_screen.dart — _showCreateMatchSheet now calls _playerService.createMatch() to persist to Supabase and uses the returned UUID as the match ID (was using a fake local timestamp ID)
- Added: screens/roster_screen.dart — _loadMatches() method that fetches existing matches from Supabase on initState so the Matches tab is populated when returning to the screen

v1.12 03.01.2026 Add match view screen with match settings menu
- Added: screens/match_view_screen.dart — full-screen view for a single match showing teams, date, location, and notes; top-right PopupMenuButton with Match Settings option opening a bottom sheet with Edit Match and Delete Match actions
- Changed: screens/matches_screen.dart — match cards are now tappable (InkWell); tapping navigates to MatchViewScreen; added clipBehavior to Card for ripple clipping

v1.12 03.01.2026 Persist matches to Supabase
- Added: Supabase_script.md — SQL to create public.matches table with RLS (team members can SELECT; coaches/owners/managers can INSERT/UPDATE/DELETE)
- Changed: models/match.dart — added teamId, fromMap, toMap, copyWith; now maps to DB columns (team_id, my_team_name, opponent_name, match_date, is_home, notes)
- Changed: services/player_service.dart — added getMatches, createMatch, updateMatch, deleteMatch CRUD methods for public.matches

v1.12 03.01.2026 Add Create Event form and match list to Matches tab
- Added: models/match.dart — in-memory Match model (id, myTeamName, opponentName, date, isHome, notes)
- Changed: matches_screen.dart — now accepts List<Match>; shows empty-state placeholder or sorted card list with date badge, title, Home/Away label, and notes
- Changed: roster_screen.dart — _matches state list; _showCreateMatchSheet() modal bottom sheet with opponent field, date picker, Home/Away ChoiceChip toggle, notes field, and Save button; MatchesScreen receives _matches

v1.12 03.01.2026 Add Roster/Matches bottom nav and Create Event FAB to RosterScreen
- Added: matches_screen.dart — placeholder Matches tab with empty-state UI
- Changed: roster_screen.dart — added BottomNavigationBar (Roster / Matches tabs); FAB switches to "Create Event" on Matches tab; existing Add Player / bulk-delete FABs preserved on Roster tab

v1.12 03.01.2026 Persist three-dot menu with logout throughout entire app flow
- Changed: calendar_screen.dart — added AuthService + PlayerService, logout handler, and AppBar PopupMenuButton with Account Settings and Log Out
- Changed: game_roster_screen.dart — added AuthService, logout handler, and merged logout + Account Settings into existing settings PopupMenuButton (icon changed to more_vert)
- Changed: saved_roster_screen.dart — added AuthService, logout handler, and AppBar PopupMenuButton with Account Settings and Log Out
- Changed: manage_members_screen.dart — added AuthService, logout handler, and AppBar PopupMenuButton with Account Settings and Log Out
- Changed: add_player_screen.dart — added AuthService, logout handler, and AppBar PopupMenuButton with Account Settings and Log Out
- Changed: player_self_view_screen.dart — added Account Settings item and more_vert icon to existing logout menu; added account_settings_screen import

v1.12 03.01.2026 Add bottom navigation bar with Teams and Schedule tabs
- Added: main_shell.dart — MainShell widget wrapping authenticated screens in a BottomNavigationBar with IndexedStack (preserves state across tab switches)
- Added: calendar_screen.dart — Schedule screen with a navigable month-view calendar grid, day selection, and events placeholder list
- Changed: auth_wrapper.dart — routes authenticated sessions to MainShell instead of TeamSelectionScreen directly; TeamSelectionScreen is now tab 0

v1.12 03.01.2026 Rate-limit redeem_team_invite RPC to thwart invite-code scraping
- Added: invite_attempt_log table (auth_uid, attempted_at) with RLS enabled and no direct-access policies
- Added: index idx_invite_attempt_log_uid_ts on (auth_uid, attempted_at DESC) for efficient windowed counts
- Changed: redeem_team_invite RPC — every call (including failures) is logged before redemption logic runs; callers exceeding 10 attempts in a 15-minute sliding window receive a rate-limit exception
- Added: optional pg_cron snippet in supabase_script.md to purge attempt rows older than 24 hours
- Updated: supabase_schema.json — new invite_attempt_log table entry and updated redeem_team_invite definition

v1.12 03.01.2026 Sanitize PostgrestException errors to prevent schema leakage
- Fixed: player_service.dart — 18 catch blocks that used throw Exception('...: $e') now route through _dbError() or an inline PostgrestException check, ensuring table names, column names, and constraint details never reach the UI
- Fixed: auth_service.dart updateProfile() — bare DB call now wrapped in on PostgrestException catch that throws Exception('Update failed.')
- Fixed: player_service.dart getJerseyNumbers() — added missing try/catch with _dbError()
- Changed: methods with intentional domain exceptions (getTeams, deleteTeam, updateMemberRole) use if (e is PostgrestException) _dbError(...); rethrow pattern to preserve user-readable guards while still sanitizing DB errors
- Changed: linkPlayerToAccount fallback now throws Exception(e is PostgrestException ? 'Update failed.' : 'Error linking player.') to preserve specific RPC messages while blocking schema leakage

v1.12 02.28.2026 Add COPPA age-gate checkbox to sign-up form
- Added: _ageConfirmed bool state to LoginScreen, reset on mode toggle
- Added: "I confirm I am 13 or older" checkbox row displayed in sign-up mode only
- Changed: _handleSubmit() now blocks sign-up and shows a snackbar if the age checkbox is unchecked

v1.12 02.28.2026 Sanitize jersey number input to prevent XSS and DB bloat
- Changed: add_player_screen.dart jersey number field now uses FilteringTextInputFormatter.allow(RegExp(r'[A-Za-z0-9]')) to block non-alphanumeric characters (e.g., <, >, ", ', ;) at the keyboard level
- Changed: jersey number max length reduced from 10 to 4 characters (covers 0–999 plus one letter suffix such as 12A)
- Added: _sanitizeJersey() helper strips non-alphanumeric chars and clamps to 4 chars at save time as defense-in-depth against programmatic API calls that bypass UI formatters

v1.12 02.28.2026 Fix email enumeration vulnerability in changeEmail
- Fixed: auth_service.dart changeEmail() no longer leaks whether an email is already registered via Postgres duplicate-key/unique-constraint error messages
- Changed: all failure paths in the RPC catch block return the same generic message ("Email change failed. Please try again later.") regardless of cause
- Changed: removed raw $e/$msg from user-facing exception strings to prevent Postgres error text from surfacing in the UI
- Changed: debugPrint calls no longer include email addresses or raw Postgres error strings

v1.12 02.28.2026 Disable OfflineCacheService on web target
- Changed: offline_cache_service.dart — all public methods (init, writeList, readList, lastUpdated, invalidate, clearAll, evictExpired) now return early (no-op / null) when kIsWeb is true
- Reason: web browsers provide no secure on-device keystore; persisting PII in browser storage is inappropriate; web always fetches fresh data from Supabase
- No caller changes required; kIsWeb is a compile-time constant so the dead code is tree-shaken on non-web builds

v1.12 02.28.2026 Replace shared_preferences with flutter_secure_storage for PII
- Changed: offline_cache_service.dart now uses flutter_secure_storage (iOS Keychain / Android EncryptedSharedPreferences) instead of shared_preferences so cached player and game-roster data (PII) is encrypted at rest
- Changed: pubspec.yaml replaces shared_preferences ^2.3.2 with flutter_secure_storage ^9.2.2
- Removed: _ensureInitialised() / SharedPreferences instance management; replaced by stateless FlutterSecureStorage instance
- Changed: clearAll() and evictExpired() now iterate via readAll() (returns Map<String,String>) instead of getKeys()

v1.12 02.28.2026 Remove athlete ID
- Removed: athlete_id field from Player model, AppUser model, AuthService (signUp, updateProfile, getCurrentUserProfile), PlayerService (_kUserColumns, _kPlayerColumns), add_player_screen.dart (controller, UI field, pre-fill, save logic), login_screen.dart (controller, UI field, sign-up call), roster_screen.dart (subtitle and detail dialog display)
- Changed: Supabase_script.md updated with ALTER TABLE migration to drop athlete_id from public.players and public.users
- Changed: security_compliance.md updated to remove athlete_id reference

v1.12 02.28.2026 Remove grade level
- Removed: grade and grade_updated_at columns from Player model, player_service.dart (_kPlayerColumns), add_player_screen.dart (grade dropdown UI + _selectedGrade state), roster_screen.dart (grade display in subtitle and detail dialog)
- Changed: supabase_schema.json updated to remove grade columns from players table
- Changed: Supabase_script.md updated with ALTER TABLE migration to drop grade and grade_updated_at columns
- Changed: security_compliance.md updated to remove grade-level references from COPPA and FERPA sections

v1.12 02.28.2026 Atomic sole-owner guard for remove_member_from_team
- Added: remove_member_from_team(p_team_id uuid, p_user_id uuid) SECURITY DEFINER RPC — performs caller auth check, sole-owner count (FOR SHARE row lock), player un-link, and membership DELETE inside a single transaction to prevent TOCTOU race where two concurrent removals could both pass the owner count and leave the team ownerless
- Changed: PlayerService.removeMemberFromTeam() now delegates entirely to the remove_member_from_team RPC; all client-side owner count queries removed
- Fixed: security_compliance.md TOCTOU finding for removeMemberFromTeam marked resolved

v1.12 02.28.2026 Scrub deleted player data from historical game rosters
- Added: scrub_deleted_player_from_rosters(p_player_id uuid) Postgres helper — removes all entries matching the deleted player from starters and substitutes JSONB arrays across every game_rosters row
- Added: delete_player(p_player_id uuid) SECURITY DEFINER RPC — authorises caller (owner/coach/team_manager), scrubs rosters, then deletes the player row
- Changed: delete_account() RPC now scrubs all players linked to the account from historical game rosters before removing auth/profile rows
- Changed: PlayerService.deletePlayer() now calls the delete_player RPC instead of a direct table DELETE
- Changed: PlayerService.bulkDeletePlayers() now delegates to deletePlayer() per-ID so every deletion goes through the scrub path

v1.12 02.28.2026 Cache wipe on all sign-out paths
- Fixed: account_settings_screen._signOut() now calls _playerService.clearCache() before signOut() — previously skipped
- Fixed: reset_password_screen._submit() now calls _playerService.clearCache() before auth.signOut() — previously skipped, also bypassed AuthService entirely
- Result: in-memory and disk caches are wiped on every sign-out path, preventing stale data leaking between accounts on a shared device

v1.12 02.28.2026 Auto-populate players on invite code redemption
- Fixed: added DROP FUNCTION IF EXISTS before CREATE OR REPLACE in supabase_script.md to resolve "cannot change return type of existing function" error on redeploy

v1.12 02.28.2026 Auto-populate players on invite code redemption
- Changed: redeem_team_invite RPC now inserts a players row pre-filled with first_name, last_name, and athlete_email from public.users, with user_id already linked, when a user redeems an invite code
- Changed: team_members row is inserted with player_id already set, so the link is established immediately
- Result: coaches only need to fill in jersey number, grade, position, etc. — no manual player creation or linking required

v1.12 02.28.2026 2205 Supabase schema consolidation
- Added: supabase_script.md — single SQL script that exports blueprint, functions, policies, triggers, foreign keys, indexes, views, and RLS status into one JSON (supabase_schema.json)
- Added: supabase_schema.json — consolidated Supabase snapshot replacing the three separate files
- Removed: supabase_blueprint.json, supabase_functions.json, supabase_policies.json

v1.12 02.28.2026 2150 Team invite system bug fix
- Fixed: redeem_team_invite RPC threw "column reference team_id is ambiguous" error; resolved by renaming RETURNS TABLE OUT parameters to out_team_id/out_team_name/out_role and correcting t.name to t.team_name
- Fixed: redeemTeamInvite() in player_service.dart updated to read out_team_id and out_team_name keys returned by the revised RPC

v1.12 02.28.2026 Team invite system
- Added: Team invite code feature — coaches and owners can generate a 6-character alphanumeric code from the Roster screen overflow menu ("Invite to Team")
- Added: Invite dialog displays the code in a styled box with expiry countdown, a Copy Code button, and an End Invite button to revoke early
- Added: Invite codes expire automatically after 24 hours; only one active code per team at a time
- Added: Supabase table team_invites with RLS (managers only) and a partial unique index enforcing one active invite per team
- Added: get_or_create_team_invite(p_team_id) RPC — returns existing active code or creates a new one
- Added: revoke_team_invite(p_team_id) RPC — deactivates the current invite code
- Added: redeem_team_invite(p_code) RPC — validates code, checks for duplicate membership, inserts team_members row with role=player, returns team_id and team_name
- Added: PlayerService.getOrCreateTeamInvite(), revokeTeamInvite(), redeemTeamInvite()
- Added: "Add Team" FAB on Team Selection screen now expands into two options: "Enter Code" (above) and "Create a Team" (same position); tapping the body collapses the menu
- Added: Enter Code dialog — 6-char input with inline validation, loading state, readable error messages for invalid/expired codes and duplicate membership; refreshes team list on success
- Changed: FAB label changed from "New Team" → "Add Team"

v1.12 02.28.2026 2123 Jersey number uniqueness warning
- Added: Jersey number uniqueness warning in add_player_screen — when typing a jersey number already assigned to another player on the team, a non-blocking orange warning appears below the field
- Added: PlayerService.getJerseyNumbers(teamId) — lightweight DB query returning all non-null jersey numbers for a team as an uppercase Set<String>
- Changed: In edit mode, the player's own current jersey is excluded from the taken set so editing it back to the same value does not trigger a false warning

v1.12 02.28.2026 Quality of life improvements
- Added: Pencil (edit) icon on each roster player row between the attendance icon and the menu icon — opens AddPlayerScreen in edit mode directly without going through the popup menu
- Added: Duplicate option in the game roster inline menu (saved_roster_screen) — prompts for a new name pre-filled as "<original> (Copy)", validates uniqueness (case-insensitive) against existing rosters, copies starters/substitutes/starter_slots to a new row
- Added: PlayerService.duplicateGameRoster() — fetches source roster and inserts a new row with copied lineup data
- Changed: Set starter slots icon in game_roster_screen AppBar changed from tune to settings (gear)
- Changed: Gear icon now opens a popup menu with "Starter Slots" and "Change Date" options instead of directly opening the slot dialog
- Added: Change Date option in game roster settings menu — opens a dialog to set or clear the game date, persisted to the DB immediately via PlayerService.updateGameRosterMeta()
- Added: PlayerService.updateGameRosterMeta() — updates game_date on an existing roster row
- Added: lib/widgets/date_input_field.dart — reusable DateInputField widget with separate YYYY / MM / DD text boxes, auto-focus advancement, and a calendar icon that opens showDatePicker
- Changed: Game date input in New Game Roster dialog (saved_roster_screen) replaced with DateInputField
- Changed: Game date input in Change Date dialog (game_roster_screen) replaced with DateInputField with a dedicated Clear button

v1.12 02.28.2026 2044 Account settings improvements
- Added: "Sign Out" button in Account Settings screen
- Added: App version footer ("Apex On Deck v2.28.26") at the bottom of Account Settings
- Added: "Change Password" button in Account Settings screen
- Added: Password change dialog prompts for current password (verified via re-auth), new password, and confirmation — blocks update if new passwords do not match
- Added: AuthService.changePassword() re-authenticates then calls Supabase Auth updateUser() to set the new password
- Added: kAppVersion constant in main.dart

v1.12 02.28.2026 Game roster size saving and validation
- Fixed: Changing the roster size (starter slots) is now persisted when saving a game roster — `updateGameRosterLineup` includes `starter_slots` in the DB update
- Fixed: Loading a saved game roster now restores the saved starter slot count so the UI reflects the correct roster size
- Added: Saving a game roster is blocked if the number of starters exceeds the roster size — a dialog prompts the coach to bench/remove a starter or increase the roster size

v1.12 02.28.2026 Performance fixes
- Fixed: Stale in-memory team cache in PlayerService — added 5-minute TTL (_teamsCacheValid) to prevent stale data on long sessions or shared devices
- Fixed: Abandoned in-flight futures in ManageMembersScreen — replaced FutureBuilder + reassigned Future with state-driven pattern (_members, _isLoading, _loadError) and a generation counter to discard superseded results
- Fixed: Hardcoded 800ms retry delay for user ID resolution — replaced direct table query + fixed retry with SECURITY DEFINER RPC (get_current_user_id), eliminating the trigger-race concern entirely and removing the retry loop
- Fixed: Legacy name-split fallback in Player.fromMap() removed — migration is complete, _kPlayerColumns never selects the legacy name column so the code was unreachable dead code
- Fixed: Missing bounds validation in getPlayersPaginated() — added assert guards for from >= 0 and to >= from
- Fixed: Triple-allocation list chain in _mapPlayers() — replaced .cast().map().toList() with single-pass .map().toList() to eliminate intermediate CastList allocation

v1.12 02.28.2026 1840
- Added: Athlete Email field in edit mode on AddPlayerScreen (re-attempts account linking on save)
- Changed: CLAUDE.md build commands split into distinct Publish, Reset, and Debug workflows
- Changed: Commands.txt updated to clarify ship vs reset workflows
