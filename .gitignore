# -------------------------------------------------------------------
# SweatDex Optimized .gitignore
# -------------------------------------------------------------------

# --- Flutter / Dart Defaults ---
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/
/coverage/
lib/generated_plugin_registrant.dart

# --- BUILD FOLDER LOGIC ---
# Ignore all build artifacts by default to keep the repo small
/build/*
# EXCEPTION: Allow the web build so we can deploy to GitHub Pages
!/build/web/

# --- Android / iOS / Linux / macOS / Windows ---
/android/app/debug
/android/app/profile
/android/app/release
/ios/Flutter/.last_build_id
/linux/flutter/ephemeral
/windows/flutter/ephemeral
app.*.symbols
app.*.map.json
*.iml
*.ipr
*.iws
.idea/

# --- Environment & Secrets (CRITICAL) ---
# [Inference] These files likely contain API keys or private database URLs
.env
oldenv
config.json

# --- Local Workspace & Notes ---
# [Inference] Based on your sidebar: temporary files that don't belong in production
tmp.txt
.history
.vscode/

# --- OS Generated ---
.DS_Store
Thumbs.db